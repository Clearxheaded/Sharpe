services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sharpe
      POSTGRES_PASSWORD: sharpe
      POSTGRES_DB: sharpe
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
